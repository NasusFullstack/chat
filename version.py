"""앱 버전.

새 릴리즈를 낼 때는 이 값을 올리고 커밋한 뒤, 반드시 같은 값으로 git 태그를 만들어 푸시할 것
(예: 이 값을 "1.0.1"로 바꾸고 커밋 -> git tag v1.0.1 -> git push origin v1.0.1).
태그를 푸시하면 GitHub Actions가 자동으로 exe를 빌드해 릴리즈에 올리고,
updater.py는 그 릴리즈 태그와 이 값을 비교해서 새 버전인지 판단함.
"""
APP_VERSION = "2.0.13"

# 이 버전을 릴리즈한 날. 환경설정 > 정보에 표시된다. 버전을 올릴 때 같이 고칠 것
RELEASE_DATE = "2026-08-12"


# 버전에 "-beta"가 붙어 있으면 테스트 버전이다. 이 값 하나로 앱 전체가 갈린다:
#   - 창 제목에 (베타)가 붙는다        -> 어느 쪽을 보고 있는지 헷갈리지 않게
#   - 설정/기록 파일 이름이 달라진다    -> 정식 버전 설정을 건드리지 않게
#   - 중복 실행 방지 자리도 달라진다    -> 정식과 테스트를 동시에 켜둘 수 있게
#   - 자동 업데이트를 하지 않는다       -> 테스트 버전은 사람이 직접 받아서 쓴다
IS_BETA = "beta" in APP_VERSION.lower()
