# 확인 대기 / 남은 일

커밋 메시지에만 남기면 "고쳤는데 실사용 확인이 안 된" 것들이 잊혀져서 같은 걸 다시 묻게 된다.
여기서만 관리한다. 확인되면 줄을 지운다.

## 실사용 확인 대기

- [ ] **채팅 공백 폭발** (v1.9.1) — 창을 좁혔다 넓히거나 대화가 길어져도 맨 아래에서
      메시지가 계속 보이는지. 예전 증상: 화면 전체가 빈 공간, 친 메시지도 안 보임
- [ ] **참여자 목록 비워짐** (v1.9.1) — 목록이 통째로 비는 일이 다시 없는지
- [ ] **움짤 세로 잘림** (v1.9.0) — `setScaledContents` 방식으로 바꿨지만 **재현을 못 해서
      미확인**. 오프스크린/화면 캡처 어느 쪽으로도 증상이 안 나옴. 다시 보이면 그때
      `friendchat_error.log`와 함께 알려줄 것
- [ ] **자동 재접속** (v1.9.0) — 서버가 실제로 끊겼을 때 안내가 뜨고 스스로 돌아오는지

## 알려진 것 / 나중에

- 낡은 테스트 몇 개는 저장소로 안 옮겼다. 없어진 API(`chat_page.log`, `chat_page._logs`)를
  쓰는 것들로, v1.8.1 기준으로 돌려도 똑같이 실패하는 것을 확인했다. 필요해지면 다시 짤 것
- IRC 대신 자체 서버만 쓰는 방향 얘기가 나왔음(2026-08-03). 결정된 것은 없음
