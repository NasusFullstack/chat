# 확인 대기 / 남은 일

커밋 메시지에만 남기면 "고쳤는데 실사용 확인이 안 된" 것들이 잊혀져서 같은 걸 다시 묻게 된다.
여기서만 관리한다. 확인되면 줄을 지운다.

## 실사용 확인 대기

- [ ] **v1.9.3의 화면 변경분 전체** — 아래 확인은 전부 **v1.9.2에서 받은 것**이라 1.9.3은
      아직 아무도 안 써봤다. 웃는 얼굴 버튼, 보관함 3x4 격자와 이름 붙임, 주소 직접 추가,
      채팅 이모티콘 192px, 사이드바 아래 로고/버전/만든이, 채널 화살표
- [ ] **참여자 목록 비워짐** (v1.9.1) — 목록이 통째로 비는 일이 다시 없는지. 원인(353 없이
      366만 오는 경우)은 고쳤지만, 원래 가끔 나던 증상이라 한동안 안 나야 확인된 것으로 본다
- [ ] **자동 재접속** (v1.9.0) — 서버가 실제로 끊겼을 때 안내가 뜨고 스스로 돌아오는지.
      테스트로는 통과했지만 실제 끊김 상황을 아직 못 겪음

## 확인된 것 (2026-08-04, **v1.9.2에서 확인**)

- 채팅이 통째로 안 보이던 것 (v1.9.1) — 창 크기를 바꿔도 안 사라짐
- 움짤 세로 잘림 (v1.9.0) — 이모티콘 움짤이 온전히 움직임. **끝까지 재현이 안 되던 건이라
  사용자 확인으로만 닫을 수 있었다**
- 이모티콘 저장/사용 (v1.9.2)

## 알려진 것 / 나중에

- 채널은 10개까지만 들어가진다. 우리 제한이 아니라 접속한 IRC 서버 규칙(`CHANLIMIT=#:10`,
  넘으면 서버가 405를 돌려줌). 늘리려면 서버 관리자가 UnrealIRCd 설정을 바꿔야 한다
- 낡은 테스트 몇 개는 저장소로 안 옮겼다. 없어진 API(`chat_page.log`, `chat_page._logs`)를
  쓰는 것들로, v1.8.1 기준으로 돌려도 똑같이 실패하는 것을 확인했다. 필요해지면 다시 짤 것
- IRC 대신 자체 서버만 쓰는 방향 얘기가 나왔음(2026-08-03). 결정된 것은 없음
- 이모티콘을 다른 PC에서도 쓰려면 보관함을 서버에 둬야 한다. 지금 서버(UnrealIRCd)는 계정에
  임의 텍스트를 저장할 방법이 없어서, 봇을 하나 붙이거나 자체 서버로 갈 때 할 일
