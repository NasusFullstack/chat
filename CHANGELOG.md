# 변경 내역

버전마다 무엇이 바뀌었는지 적어둡니다. 설치 파일은 [릴리즈 페이지](https://github.com/NasusFullstack/chat/releases)에서 받을 수 있고, 앱은 켤 때 새 버전을 자동으로 확인합니다.

---

## v2.2.0
**이제 기본이 보안 접속(암호화)입니다.**

- 예전에는 모두 평문(6667)으로 접속해서 **대화와 비밀번호가 그대로 오갔습니다**.
  같은 와이파이에 있는 사람이 들여다볼 수 있는 상태였습니다
- 보안 접속(6697)이 앱에서 안 되던 원인을 고쳤습니다. 개인 서버는 정식 인증서가 없는
  경우가 많은데, 앱이 무조건 거부하고 있었습니다. 이제 **처음 한 번만 확인을 묻고
  기억합니다**(다른 IRC 프로그램과 같은 방식). 나중에 인증서가 바뀌면 다시 물어봅니다
- 예전에 평문으로 접속하던 설정도 자동으로 보안 접속으로 올라갑니다
- 보안 접속이 막힌 네트워크(학교·회사)에서는 **한 번 일반 접속으로 물러납니다**

## v2.1.5
- **서버 계정을 앱에서 바로 만들 수 있습니다.** 채팅창에 `/register 비밀번호 이메일`.
  계정을 만들면 비밀번호 접속이 되고, 회선이 바뀌어 이름이 밀려도 즉시 되찾습니다
- **"SASL 인증 실패"가 뭘 하라는 건지 알려줍니다.** 예전에는 서버 원문만 떠서 무섭기만 했는데,
  이제 "계정이 없거나 비밀번호가 다릅니다 → /register로 만드세요"라고 안내합니다
  (실패한 비밀번호로 다시 시도하지 않아 오류 문구도 한 줄만 뜹니다)
- **환경설정 톱니가 다시 왼쪽 아래**로 갔습니다(접어도 같은 자리)
- **채널 목록을 접고 펼 때 깜빡이지 않습니다.** 대화창이 한 번 커졌다 돌아오던 현상 제거

## v2.1.4
- **비밀번호 로그인이 안전해지고 빨라졌습니다.** 접속 과정에서 바로 인증합니다(SASL).
  예전에는 접속한 뒤 비밀번호를 채팅 메시지로 보냈습니다
- **채널 목록을 접고 펼 때 창의 왼쪽만 움직입니다.** 오른쪽(대화창·참여자)이 끌려다니지 않습니다
- **접었을 때 환경설정 톱니가 화살표 바로 아래**에 붙습니다(구석에 떨어져 있지 않게)
- 서버가 이상한 값을 보내도 앱이 멈추지 않도록 정비했습니다

## v2.1.3
- **이름이 밀렸을 때 곧바로 되찾습니다.** 비밀번호를 넣어두면 접속 1~2초 안에 원래 이름으로
  돌아옵니다(예전에는 최대 15초 기다렸습니다)
- 이름을 영영 못 되찾는 경우(다른 사람이 진짜로 그 이름을 쓰는 중)에는 시도 간격을 늘리다가
  멈춥니다 - 계속 두드리면 서버가 연결을 끊을 수 있습니다

## v2.1.2
- **이름이 밀렸을 때 즉시 되찾습니다(등록된 이름 한정).** 회선이 끊기듯 바뀌면 서버는
  최대 180초 동안 예전 접속을 붙잡고 있어 이름 뒤에 `_`가 붙습니다. 로그인에 비밀번호를
  넣어두면 서버 서비스(NickServ)에 요청해 그 즉시 되찾습니다
  (비밀번호가 없으면 예전처럼 유령이 사라지는 순간 자동으로 돌아옵니다)

## v2.1.1
**장문·줄바꿈 문제를 뿌리부터 고쳤고, 회선이 바뀌어도 이름을 지킵니다.**

- **긴 글이 제대로 보입니다.** 글자 배치와 높이를 텍스트 엔진이 맡도록 구조를 바꿨습니다.
  띄어쓰기 없이 길게 써도 접히고, 잘리거나 빈 공간이 생기지 않습니다
- **긴 글이 상대에게 잘려서 가던 문제를 고쳤습니다.** IRC는 한 줄이 512바이트를 넘으면
  서버가 잘라버립니다(한글 150자쯤). 이제 알아서 나눠 보냅니다
- **와이파이↔랜선을 바꿔도 이름 뒤에 _가 붙지 않습니다.** 예전 접속이 서버에 잠깐 남아
  있어서 생기던 문제인데, 원래 이름을 기억해뒀다가 되찾습니다
- **유튜브 링크에 미리보기가 뜹니다**(제목·채널·썸네일)
- **트레이 아이콘이 사라져도 돌아옵니다.** 탐색기가 재시작되면 다시는 안 보이던 문제
- 환경설정 톱니를 채널 목록 밖으로 옮겨 **접어도 항상 보입니다**(모양도 또렷하게)
- **채널 목록을 펴도 대화창이 좁아지지 않습니다** - 창이 그만큼 넓어집니다

## v2.0.17
**앱이 갑자기 멈추고 꺼지던 진짜 원인을 찾아 고쳤습니다.**

- 창 가장자리에서 마우스를 움직일 때 처리가 **스스로를 무한히 파고들어** 앱이 통째로
  죽고 있었습니다(기록: `Windows fatal exception: stack overflow`). v2.0.14에 넣어둔
  크래시 기록 덕분에 정확한 위치를 찾았습니다
- **장문이 줄바꿈되지 않고 안 보이던 문제도 고쳤습니다.** 띄어쓰기 없이 길게 쓰면
  Qt가 아예 접지 않아 한 줄로 굳었고, 그 줄의 높이도 화면 크기에 눌려서 스크롤조차
  생기지 않았습니다. 이제 접히고, 끝까지 스크롤됩니다

## v2.0.16
**연결이 조용히 죽으면 스스로 알아채고 다시 붙습니다.**

- 서버는 90초마다 확인 신호를 보내고 **180초 동안 답이 없으면 끊습니다**("Ping timeout: 180 seconds").
  노트북이 잠들거나 와이파이가 연결을 버리면 우리 화면에는 멀쩡히 접속된 것처럼 보이는데
  실제로는 아무 것도 오가지 않고, 그동안 보낸 메시지는 사라집니다
- 이제 120초 넘게 조용하면 우리가 먼저 확인 신호를 보내고, 170초까지 답이 없으면
  **서버가 끊기 전에** 스스로 다시 연결합니다
- 정상적으로 대화가 오가는 동안에는 아무 것도 더 보내지 않습니다(서버 부담 없음)

## v2.0.15
- **링크를 붙였을 때 앱이 멈추던 문제를 고쳤습니다.** 어떤 쇼핑몰 주소를 채팅에 올리면
  미리보기를 만드는 도중 **13.7초 동안 화면이 통째로 멈췄습니다**(페이지가 클수록 더 오래).
  이제 아무리 큰 페이지가 와도 즉시 끝납니다
- **서버 환영 인사가 제대로 보입니다.** IRC의 색·굵게 표시를 우리 앱이 몰라서
  `11⟦ PDLab. IRC ⟧` 처럼 색 번호가 글자로 튀어나오고 오류 문구처럼 보였는데,
  이제 다른 IRC 클라이언트처럼 **색이 입혀진 안내**로 보입니다
- 다른 사람이 색을 넣어 보낸 메시지도 그 색 그대로 보입니다

## v2.0.14
**"앱이 갑자기 멈추더니 꺼진다"는 증상을 추적할 수 있게 했습니다.**

- 이런 종류는 프로그램이 통째로 죽는 것이라 지금까지 **기록이 한 줄도 안 남았습니다.**
  이제 죽는 순간과, 화면이 멈춘 채로 20초가 지나는 순간에 **어디서 멈췄는지**를
  설치 폴더의 `friendchat_error.log`에 남깁니다
- 아직 원인을 고친 것은 아닙니다. 다음에 그 증상이 나면 그 기록으로 원인을 찾습니다

## v2.0.13
v2.1.0에서 앱이 아예 안 켜졌던 사고를 바로잡고, 테스트 버전으로 먼저 검증한 뒤 올린 첫 정식 버전입니다.

- **한 컴퓨터에서 하나만 실행됩니다.** 두 번 켜면 이미 떠 있는 창을 꺼내줍니다(트레이 아이콘이 두 개 생기던 문제)
- **업데이트가 끝나면 무엇이 바뀌었는지 창으로 보여줍니다.** 버전당 한 번, 저절로 닫히지 않습니다
- **그 내용을 언제든 다시 볼 수 있습니다.** 환경설정 > 정보 > "이 버전 변경 내역 보기"
- **채팅창에도 한 줄 남습니다.** 창을 닫아도 무엇이 바뀌었는지 그 자리에 남습니다(나에게만 보입니다)
- **앱을 끄면 채팅 서버에서 즉시 나갑니다.** 예전에는 한참 뒤 "Ping timeout"으로 빠져서, 그동안 참여자 목록에 남아 있었습니다
- 알림 설정 중 "사람만 표시 / 메시지만 표시"가 재시작하면 무시되던 버그 수정(테마 설정도 같은 문제였습니다)
- 접속이 안 될 때, 네트워크가 채팅 포트를 막은 경우인지 확인해서 알려줍니다(학교·회사 와이파이)
- **새 기능은 이제 테스트 버전으로 먼저 나갑니다.** 정식 버전에는 확인이 끝난 뒤에 올라갑니다

## v2.1.0 (내려감)
- **한 컴퓨터에서 하나만 실행됩니다.** 두 번 켜면 트레이 아이콘이 두 개 생기고 알림도 두 번 왔는데, 이제 두 번째로 켜면 새로 뜨지 않고 **이미 떠 있는 창을 꺼내줍니다**
- **업데이트가 끝나면 무엇이 바뀌었는지 창으로 보여줍니다.** 한 버전당 딱 한 번만 뜨고, **저절로 닫히지 않습니다**(읽는 중에 사라지면 다시 볼 방법이 없으니까요)
- 알림 설정 중 **"사람만 표시 / 메시지만 표시"를 골라도 다시 켜면 무시되던 버그** 수정. 설정을 읽어올 때 글자 값이 참/거짓으로 뭉개지고 있었습니다(테마 설정도 같은 문제였습니다)

## v2.0.11
**어떤 이미지는 안 뜨고 그 자리가 빈칸이 되던 문제를 고쳤습니다.**

- 그림은 제대로 받아와 있는데도 메시지 줄 높이가 글자 높이(34px)에서 굳어, 그림이 안 보이고 그 자리가 통째로 비었습니다
- 그림이 **레이아웃 계산보다 늦게 도착하면** 생기는 문제라 "되는 이미지와 안 되는 이미지"가 갈렸습니다. 용량이 큰 이미지일수록 잘 걸렸습니다
- 같은 이유로 뉴스/게시물 카드가 늦게 뜰 때도 같은 증상이 있었는데 함께 해결됩니다

## v2.0.10
- 확인이 미뤄진 사람들은 **쿨타임이 풀리는 시점에 자동으로 한 번 더 확인**합니다. 예전에는 그 사이에 들어온 사람들이, 그 뒤로 아무도 들락거리지 않으면 계속 비어 있었습니다
- 여러 명이 몰려 들어와도 확인은 **한 번에 묶어서** 합니다
- 끝까지 답하지 않는 사람에게는 다시 묻지 않습니다(응답을 꺼둔 사람일 수 있고, 그런 사람에게 계속 묻는 건 채널 전체에 소음입니다)

## v2.0.9
**나중에 들어온 사람도 로고가 뜹니다.**

- 예전에는 채널에 처음 들어갈 때 한 번만 확인해서, 그 뒤에 들어온 사람은 계속 비어 있었습니다
- **같은 이름으로 다른 프로그램을 켜고 다시 들어오면 다시 확인합니다.** 예전 기억을 그대로 쓰면 엉뚱한 로고가 며칠 붙어 있게 됩니다
- 혼자 들어오면 그 사람에게만 조용히 묻고, 여러 명이 모르는 상태면 채널에 한 줄만 보냅니다(몇 분에 한 번으로 제한)

## v2.0.8.1
- 참여자 목록의 로고가 **오른쪽 끝에 붙습니다.** 안쪽으로 밀려 들어와 긴 닉네임과 겹쳐 보이던 것 수정

## v2.0.8
**휴대폰 접속을 훨씬 잘 잡습니다.** 그리고 로고들이 세로로 줄을 맞춥니다.

- 프로그램 목록에 없어도 응답에 `Android`/`iOS`/`iPhone` 같은 말이 있으면 휴대폰으로 봅니다
- 응답에 아무 단서가 없어도 **닉네임에 `mobile`·`phone`·`droid`가 들어 있으면** 휴대폰으로 봅니다. IRCCloud처럼 웹에서 쓰든 휴대폰에서 쓰든 똑같이 답하는 경우가 있어서, 이름이 유일한 단서입니다
- 이름이 `~Bot`으로 끝나거나 응답에 `bot`/`relay`/`bridge`가 있으면 봇으로 봅니다
- **로고 칸과 표시 칸의 자리를 고정**했습니다. 예전에는 표시가 붙은 줄만 로고가 왼쪽으로 밀려서 지그재그로 보였습니다
- Quasseldroid · AndroIRC · Yaaic · LimeChat · Atomic 추가

## v2.0.7
**로고 옆에 종류 표시가 따로 붙습니다.** 로고 위에 겹쳐 그리면 작아서 안 보이기 때문에 자리를 따로 줬습니다.

- PC에서 접속했으면 로고 하나만 (표시 없음)
- **휴대폰에서 접속했으면 로고 옆에 회색 휴대폰** — 예: WeeChat Android는 WeeChat 로고 + 휴대폰
- **봇·서비스면 로고 옆에 회색 로봇** — 디스코드 연결 계정도 봇이므로 디스코드 로고 + 로봇으로 뜹니다
- 종류 표시는 회색입니다. 브랜드 로고와 색이 겹치면 무엇이 진짜 프로그램인지 헷갈리기 때문입니다

## v2.0.6
알아보는 프로그램을 **16종 → 30종**으로 늘리고, 종류별로 다르게 보여줍니다.

- **로고가 뜨는 것**: 춥채팅 · Discord · WeeChat · HexChat · irssi · mIRC · The Lounge · Kiwi IRC · **IRCCloud · Konversation · KVIrc · senpai · ZNC**
- **봇과 서비스는 로봇 모양**으로 표시합니다. NickServ·ChanServ 같은 서비스(Anope/Atheme), Eggdrop·Sopel·Limnoria 같은 봇, 그리고 봇 라이브러리로 접속한 계정까지. 사람이 아니라는 게 한눈에 보입니다
- **휴대폰에서 접속한 사람은 휴대폰 모양**으로 표시합니다. Goguma · Palaver(iOS) · Revolution IRC · AndChat · HoloIRC · Igloo · Mutter · Colloquy · WeeChat Android
- 이름이 `~Serv`로 끝나면(NickServ, ChanServ 등) 답을 안 해도 서비스로 봅니다
- Textual · Quassel · Halloy는 아직 글자 배지입니다(공식 로고 주소를 못 찾았습니다)

## v2.0.5
**접속 프로그램 확인 방식을 바꿔서, 막히던 서버에서도 제대로 뜹니다.**

- 모르는 사람이 여럿이면 **채널에 한 줄만** 물어봅니다. 사람마다 따로 보내면 서버가 막던 문제(`Multi-target messaging is not allowed`)가 사라지고, 보내는 줄도 사람 수만큼에서 한 줄로 줄었습니다
- **내 계정에도 로고가 뜹니다** (물어볼 필요 없이 우리 프로그램이니까요)
- 디스코드로 이어진 계정은 자기가 쓰는 라이브러리 이름(`girc` 등)을 답하는데, 그대로 믿지 않고 디스코드로 표시합니다
- 서버가 거절할 때 뒤늦게 도착하던 경고가 채팅창에 한 줄 더 남던 것도 안 보이게 했습니다

## v2.0.4.1
- **디스코드로 이어진 계정에 디스코드 로고가 뜹니다.** 그 계정은 사람이 쓰는 IRC 프로그램이 아니라 채널을 이어주는 봇이라, 물어봐도 디스코드라고 답하지 않습니다. 그래서 이름으로 알아봅니다(툴팁에 "이름으로 짐작"이라고 밝힙니다)
- 모르는 프로그램의 배지가 **숫자 7처럼 보이던 것** 수정. 물음표를 그렸는데 작은 크기에서 곡선이 뭉개졌습니다. 이제 그 프로그램 이름의 첫 글자를 보여줍니다
- 마우스를 올리면 상대가 답한 **원문 그대로** 보여줍니다(무엇인지 알려주면 다음 버전에서 로고를 붙일 수 있습니다)

## v2.0.4
접속 프로그램을 알아보는 과정을 **서버와 상대에게 폐가 되지 않게** 고쳤습니다.

- **거절하는 서버에서는 자동으로 멈춥니다.** 일부 서버(UnrealIRCd 등)는 여러 사람에게 개인 메시지를 보내는 것을 막아두는데, 그런 서버에서 `Multi-target messaging is not allowed` 경고가 참여자 수만큼 채팅창에 쏟아졌습니다. 이제 한 번 거절당하면 바로 멈추고 그 서버를 기억해 다시 시도하지 않습니다
- **물어보지 않고도 알아냅니다.** 춥채팅끼리는 프로필 아이콘을 주고받을 때 서로를 알아보므로, 확인 요청이 막힌 서버에서도 우리 사용자끼리는 로고가 뜹니다
- 프로그램끼리 주고받는 응답(`ERRMSG that is an unknown CTCP query` 등)이 채팅창에 그대로 보이던 것 수정

- **한 번 알아낸 사람은 다시 묻지 않습니다.** 서버별로 기억해두고 다음에 켤 때는 바로 로고가 뜹니다(일주일마다 한 번만 다시 확인)
- **지금 보고 있는 채널의 사람에게만** 물어봅니다. 여러 채널에 들어가 있어도 한꺼번에 나가지 않습니다
- 물어보는 간격을 2초 → 3.5초로 늘리고, 한 번에 묻는 인원도 25명으로 낮췄습니다
- 채널에 막 들어간 순간(서버가 참여자 목록·지난 대화로 바쁠 때)에는 한 박자 쉬었다 시작합니다

## v2.0.3
**참여자가 무슨 프로그램으로 접속했는지 로고로 보여줍니다.** 참여자 목록 오른쪽 끝에 작게 뜨고, 마우스를 올리면 이름이 나옵니다.

- 춥채팅 / WeeChat / HexChat / irssi / mIRC / Discord(다리) / The Lounge / Kiwi IRC 등을 알아봅니다
- **채팅을 한 마디도 안 한 사람도 알 수 있습니다.** IRC 표준 방식(CTCP VERSION)으로 조용히 물어보고, 상대 화면에는 아무것도 뜨지 않습니다
- 답을 주지 않는 사람(응답을 꺼둔 경우)은 로고 없이 그대로 둡니다
- 로고는 각 프로그램 공식 사이트에서 한 번만 받아 저장해두므로, 다음부터는 바로 뜹니다
- 환경설정 > 알림에서 **끌 수 있습니다** ("참여자가 쓰는 프로그램을 로고로 표시")

## v2.0.2.2
- 참여자 목록의 **여섯 번째 사람이 반쯤 잘려 보이던 것** 수정 (테두리와 여백이 차지하는 자리를 빼지 않아 실제로는 5.7줄만 보였습니다)
- README 화면 사진을 지금 모습으로 새로 찍었습니다

## v2.0.2.1
- 트레이 아이콘 우클릭 메뉴의 '환경설정...'에서 점 세 개를 뺐습니다(`환경설정`)
- README를 지금 화면에 맞게 갱신했습니다(트레이 상주·알림 범위 선택·채널 목록 접기·참여자 목록·이모티콘 이름 표시)

## v2.0.2
환경설정을 **알림 / 테마 / 정보** 세 탭으로 나눴습니다.

- 알림 설정이 계층으로 정리됐습니다. 위 항목을 끄면 아래는 회색으로 흐려집니다
  - 새 메시지 알림 표시
    - 알림에 보낸 사람과 내용 표시
      - 사람만 표시 / 메시지만 표시 / 모두 숨김
- **사람만 표시**는 누가 보냈는지만, **메시지만 표시**는 내용만 뜹니다. 예전에는 "전부 보이기"와 "전부 숨기기" 둘뿐이었습니다
- **정보** 탭에 로고·버전·릴리즈 날짜·만든 사람·메일·GitHub·저작권을 모았습니다
- 트레이 아이콘 우클릭 메뉴에서 '알림 표시'를 없앴습니다. 같은 설정이 두 곳에 있으면 한쪽만 바뀌어 어긋나기 때문에, 알림 설정은 환경설정 창 한 곳으로 모았습니다(메뉴는 열기 / 환경설정 / 종료)

## v2.0.1
**앱이 갑자기 사라지던 문제를 고쳤습니다.** 채팅 목록의 스크롤바가 "필요할 때만" 뜨는 설정이었는데, 바가 뜨면 폭이 줄고 → 줄바꿈이 달라져 내용 높이가 바뀌고 → 바가 사라지고 → 다시 필요해지는 되풀이에 빠져 프로그램이 즉시 종료됐습니다(오류 창도 없이). 이제 채팅 스크롤바는 항상 표시됩니다.

- 채널 목록 여닫기를 **경계 가운데 화살표 손잡이**로 바꿨습니다. 접으면 목록이 완전히 사라지고 그 폭만큼 대화창이 넓어집니다
- 채널 목록 맨 아래에 **환경설정 톱니바퀴**를 넣었습니다(트레이 메뉴와 같은 창)
- 알림 팝업을 조금 키우고 화면 모서리에 더 붙였습니다. **닫기 버튼**도 생겼습니다(누르면 창은 열리지 않고 알림만 사라집니다)
- 환경설정에 **"알림에 보낸 사람과 내용 표시"** 켜고 끄기가 생겼습니다. 끄면 "새 메시지가 도착했습니다"만 떠서, 옆에서 화면을 봐도 대화 내용이 노출되지 않습니다
- 이모티콘을 고르면 입력창에 긴 주소 대신 **이름**(`[:고양이:]`)이 보입니다. 보낼 때는 그대로 이모티콘으로 나갑니다
- 이모티콘 창이 떠 있을 때 **이모티콘 버튼을 다시 누르거나 입력창을 누르면 닫힙니다**
- 배틀크루저 소환 시 그림 파일이 없을 때 쓰는 손그림이 아예 안 그려지던 버그 수정

## v2.0.0
화면 구성을 손봤습니다.

**알림**
- 새 메시지 알림이 **앱이 직접 그리는 팝업**으로 바뀌었습니다. 앱과 같은 디자인이고 누르면 창이 열립니다. 윈도우 기본 알림은 더 이상 뜨지 않습니다
- 안읽은 채널은 **채널 줄 전체가 옅은 노란색으로 깜빡이고**, 읽을 때까지 노란색이 유지됩니다 (예전의 작은 노란 점은 없앴습니다)

**채널 목록**
- 왼쪽 채널 목록을 **접었다 펼 수 있습니다.** 접으면 대화 영역이 그만큼 넓어지고, 접어둔 상태는 다음에 켤 때도 그대로입니다
- 채널마다 6px씩 쌓이던 빈 공간을 없앴습니다 (목록을 자를 때 마지막 채널이 반쯤 걸쳐 보이던 것도 같이 해결)

**참여자 목록**
- 창 끝까지 늘어나던 목록을 **6명까지만** 보이게 하고, 넘치면 얇은 스크롤바로 내려 봅니다
- 머리글에 **몇 명이 있는지** 표시합니다
- 로고·메일·GitHub 표시를 채널 목록 아래에서 **참여자 목록 맨 아래로** 옮겼습니다 (채널 목록은 접으면 사라지므로)
- 프로필 변경 버튼은 참여자 목록 바로 아래에 붙습니다

**그 밖에**
- 환경설정에 **테마 고르기**가 생겼습니다. 지금은 기본 테마 하나이고, 색 구성은 이미 테마 단위로 묶어둬서 다음 버전에서 테마가 추가되면 바로 고를 수 있습니다
- 채팅 입력창에서 글자 아래가 미묘하게 잘리던 것 수정

## v1.9.10
- 창을 닫을 때마다 뜨던 "종료되지 않습니다" 안내를 처음 한 번만 표시

## v1.9.9
- 알림 본문을 20자로 줄이고 뒤에 `...` 표시
- 연달아 온 메시지는 하나로 묶어서 **최신 것만** 표시 (여러 개가 쌓이지 않음). 묶인 게 있으면 "외 2건", 보낸 사람이 여럿이면 "외 2건 · 2명"

## v1.9.8
- 알림에 이모티콘 주소가 그대로 뜨던 것 수정 → `(이모티콘)`으로 표시

## v1.9.7
- **트레이 상주** — 창을 닫아도 종료되지 않고 계속 메시지를 받습니다. 종료는 작업표시줄 오른쪽 아이콘 메뉴에서
- **새 메시지 알림** — 창을 보고 있지 않을 때 누가 뭐라고 했는지 알려줍니다
- **환경설정** — 트레이 메뉴에서 알림 표시 / 닫으면 트레이에 두기를 켜고 끌 수 있음
- 내가 보낸 메시지는 지난 대화를 보던 중이어도 항상 맨 아래로 내려옵니다
- 사이드바 아래에 GitHub 계정 표시

## v1.9.6
- 뉴스/게시물 카드의 제목·설명이 위아래로 잘리던 것 수정
- 위로 올려 지난 대화를 읽는 중에는 새 메시지가 와도 화면이 끌려 내려가지 않음

## v1.9.4 ~ v1.9.5
- 코드 구조 정리(컴포넌트화). 기능과 화면은 그대로 — 화면 픽셀 비교로 확인했습니다

## v1.9.2 ~ v1.9.3
- **이모티콘** — 보관함에 저장해두고 골라서 보냅니다. 채팅의 이미지를 우클릭해 저장하거나, 주소를 직접 넣어 추가할 수 있고, 이름을 붙여 검색할 수 있습니다

## v1.9.1
- **채팅이 통째로 안 보이던 문제** 수정 (대화가 길어질수록 높이 계산이 어긋나 빈 공간이 화면을 덮었음)
- 참여자 목록이 통째로 비던 문제 수정
- 예상 못 한 오류를 파일로 남기기 시작 (`설치폴더\friendchat_error.log`)

## v1.9.0
- **자동 재접속** — 서버가 끊기면 알려주고, 간격을 늘려가며 다시 붙은 뒤 보던 채널로 돌아갑니다
- 움짤이 세로로 잘려 보이던 것 수정

## v1.8.x 이전
- 링크 미리보기(이미지·움짤·뉴스 카드), 채널 사이드바, `@닉네임` 호출, 슬래시 명령,
  프로필 아이콘, 자동 업데이트, 실제 IRC 서버 접속 모드
